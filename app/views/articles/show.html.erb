<h1>Show de l'article</h1>

<div>by <%= @article.user.username %> </div>
<p>
  <strong>Titre:</strong>
  <%= @article.titre %>
</p>

<p>
  <strong>Description:</strong>
  <%= @article.description %>
</p>

<% if logged_in? && (@article.user == current_user || current_user.admin?) %>
  <%= link_to 'Modifier', edit_article_path(@article) %>
  <%= link_to 'Supprimer', article_path(@article), method: :delete, date: {confimr: "Etes-vous sûr?"} %>
<% end %>
<%= link_to 'Retour', articles_path %>
