<h1>Liste des catégories</h1>

<table>
  <thead>
    <tr>
      <th>Titre</th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |categorie| %>
      <tr>
        <td><%= categorie.name %>
        <p class="card-text"><%= pluralize(categorie.articles.count, "article") %></p></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
